<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTP to Ollama on localhost / emulator / LAN (cleartext). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
